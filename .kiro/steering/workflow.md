# 工作流规则

## 命令执行权限

本工程内的所有代码相关操作（编译、构建、运行测试、文件操作等），Kiro 可以直接执行 WSL 命令行，无需等待用户确认。

**例外**：涉及 git 仓库删除操作（如 `git rm -rf`、删除 `.git` 目录等）需要用户确认。

## WSL 命令执行方式

工程路径：`/home/xuejia/work/lvgl/str`

执行命令格式：
```
wsl -e bash -c "<command>"
```
