日期：20260418
完成度：已完成
TODO：重新规划目录结构
原因：
原来在str目录下有一个platform目录，本来目的是打算做不同的平台兼容。
现在重新考虑候得出结论：并不是所有的ui应用都需要在所有的平台上运行。
因此决定，将原platform修改未基于sdl的模拟器平台目录。
在UI应用，例如box86目录下，添加platform目录，如果box86运行在lvgl模拟器环境，则链接str下的模拟器目录。
如果要在rk3506+linux环境下运行，则在box86/platform/rk3506目录下添加平台接口文件。
总结：str目录只支持sdl模拟器平台。其他运行平台在UI APP目录下进行支持。

日期：29260418
完成度：已完成
TODO：box86支持中文显示。
要求：
在box86目录下有一个/assets, 此目录用来保存字库和图片文件。
在sdl模拟器下，直接引用此路径。在rk3506平台，则保存在/usr/share/assets路径。
请修改代码和cmake文件（可以新增config.h文件），让box86支持显示中文。
并且，编译sdl模拟器和rk3506平台都能显示。适量字库使用freetype引擎。
另外，在编译rk3506平台时，请将lvgl编译成库，引用路径为/usr/lib/.

日期：29260419
完成度：已完成
TODO：整理box86 readme.md
要求：
1、编写readme.md，说明box86工程，包括大概的工程结构，配置依赖，编译说明等内容。
2、详细设计保留在DESIGN.md.
3、测试说明保留在TESTING.md，请分析TESTING.md，可以将无关内容移动到readmd.md。

日期：29260419
完成度：已完成
TODO：评估freetype和tinyttf
要求：
评估freetype和tinyfft的性能差异，包括但不限于代码量、内存（堆/栈）消耗、汉字渲染速度。将结果形成文档输出。最好能输出在嵌入式linux平台的评估。

日期：29260419
完成度：已完成
TODO：梳理lvframe和box86 边界
问题： 设备模型，是不同的ui应用定义。lvframe应该只提供数据模型的管理，而不是直接定义设备数据模型。
要求：优化frame设计，设备模型定义放到ui应用，例如boxui。frame只提供管理这些数据模型的功能。

日期：29260421
完成度：已完成
TODO：添加页面四周效果
需求：
在普通灯设备页四周添加一圈10像素的宽的边框，颜色类似lv_cpicker。
当点击开关时，设计一种炫彩灯的效果：颜色像流水一样，绕着页面旋转一圈。

日期：29260421
完成度：已完成
TODO：色温灯添加页面四周效果
需求：
1、参考普通灯四周效果。
2、参考box86/ref/pic_1.png
3、从我的角度分析pic_1.png和普通灯页面效果的区别如下：
1. 色带的内边缘有渐变过度效果。
2. 流动的不是颜色，是参考图片顶部的亮区，在点击开关时，亮区环绕四周流动。

日期：20260424
完成度：已完成
TODO: 添加屏保页功能和逻辑。
需求：
#### 系统参数
1. 无操作待机时间 默认10秒
2. 屏保维持时间   默认20秒
3. 是否打开屏保   默认打开
4. 息屏状态下点击屏幕是返回屏保还是返回用户首页。 默认返回屏保
#### 屏保逻辑
1. 如果打开屏保功能(参数3)，用户无操作时间达到无操作待机时间(参数1)，进入屏保页。
2. 如果关闭屏保功能(参数3)，用户无操作时间达到无操作待机时间(参数1)，直接息屏。
3. 逻辑1进入屏保，计时达到参数2，进入息屏状态。
4. 息屏状态点击屏幕唤醒，根据参数4，决定时返回屏保页还是返回用户首页。
5. 屏保状态下点击屏幕，退出屏保。
6. 用户首页定义：第一个设备页为用户首页。
7. 逻辑4，假如进入屏保（逻辑1）或直接息屏（逻辑2）时，不在首页（在其他设备页、下拉页、设置也），需要回到首页。
#### 额外接口
1. 提供唤醒事件接口，并且增加测试 shell命令，唤醒事件与用户点击屏幕功能一致。
#### UI页面说明
1. 屏保页需要是一个独立的页面。显示屏保二字。
2. 使用一个黑页面模拟息屏，页面添加息屏二字。
3. 请在设置也增加本需求的4个参数设置页面。
#### 特别说明
1. 息屏状态下用户点击，唤醒候，加入直接返回首页，点击事件不允许二次触发首页功能。


日期：20260506
完成度：已完成
TODO: 普通灯添加二级页面：设备信息
需求：
#### UI要求
1. 普通灯页面增加一个设备信息按钮。
2. 点击按钮，进入设备信息页，UI展示设备ID、名称、位置等信息。
3. 设备信息页背景40%透明度。
4. 点击设备信息页任何地方，都退出设备信息页，返回设备页。
