# box86 工程设计文档

## 概述

本工程是一个智能家居中控屏的 UI 应用程序，基于 LVGL 9.4 和 lvframe 页面管理框架开发。

- **UI 渲染**：基于 lvframe 的多页面管理，支持左右滑动切换设备页、下拉进入设置页等交互
- **业务逻辑**：设备数据模型管理，支持通过 Shell 命令行与 UI 双向联动
- **UI 与业务交互**：LVGL 和业务逻辑运行在各自独立的线程中，通过 lvframe 事件总线传递消息解耦。UI 操作（如点击按钮）触发消息发往业务线程，业务线程修改数据模型后输出 Shell 日志；Shell 输入指令由业务线程解析，修改模型后通知 UI 刷新。添加/删除设备是特殊操作，需严格管理生命周期：删除时必须先销毁 UI 页面再释放数据模型，防止 UI 回调访问已释放内存导致崩溃；UI 只持有设备 ID，通过只读数据快照读取模型数据，业务线程是数据模型的唯一所有者。详见「线程架构与业务交互」章节。

---

## 屏幕规格

| 参数 | 值 |
|------|----|
| 分辨率 | 480 × 480 |
| 色深 | 16bit |
| 触摸类型 | 电容屏 |

---

## 页面结构

```
┌──────────────────────────────────────────┐
│           设备页（主页面区域）              │
│   [设备页1] ←─ 左右滑动 ─→ [设备页N]      │
│   普通灯 / 色温灯 / 电动窗帘，支持多实例    │
└─────────────────┬────────────────────────┘
                  │ 仅第一页支持下拉
                  ▼
┌──────────────────────────────────────────┐
│                 设置页                    │
│        [网络开关]    [更多设置]            │
└─────────────────┬────────────────────────┘
                  │ 上拉返回首页
                  │ 点击"更多设置"
                  ▼
┌──────────────────────────────────────────┐
│              更多设置页                   │
│   [音量设置]  [屏幕设置]  ...（可滚动）    │
└─────────────────┬────────────────────────┘
                  │ 点击具体入口
                  ▼
┌──────────────────────────────────────────┐
│          具体设置页（音量 / 屏幕）          │
│          左上角返回按钮                    │
└──────────────────────────────────────────┘
```

---

## 各页面设计

### 设备页 — 普通灯

| 元素 | 类型 | 说明 |
|------|------|------|
| 设备名称 | Label | 显示设备名称（如"客厅灯"） |
| 开关按钮 | Button | 点击切换开/关状态 |
| 状态 Label | Label | 显示当前状态（"已开启" / "已关闭"） |

### 设备页 — 色温灯（继承普通灯）

| 元素 | 类型 | 说明 |
|------|------|------|
| 设备名称 | Label | 显示设备名称 |
| 开关按钮 | Button | 点击切换开/关状态 |
| 状态 Label | Label | 显示当前状态 |
| 色温滑动条 | Slider | 调节色温，范围 2700K ~ 6500K |
| 色温值 Label | Label | 实时显示当前色温值（如"4000K"） |

### 设备页 — 电动窗帘

| 元素 | 类型 | 说明 |
|------|------|------|
| 设备名称 | Label | 显示设备名称 |
| 打开按钮 | Button | 发送 OPEN 指令 |
| 关闭按钮 | Button | 发送 CLOSE 指令 |
| 停止按钮 | Button | 发送 STOP 指令 |
| 开合度 Label | Label | 显示当前开合度（如"75%"） |

### 设置页

| 元素 | 类型 | 说明 |
|------|------|------|
| 网络开关按钮 | Button | 切换网络开/关；开启时按钮为金黄色 |
| 更多设置按钮 | Button | 点击进入更多设置页 |

进入方式：仅从第一设备页下拉进入；上拉返回首页。

### 更多设置页

| 元素 | 类型 | 说明 |
|------|------|------|
| 返回按钮 | Button | 左上角，返回设置页 |
| 音量设置入口 | List Item | 点击进入音量设置页 |
| 屏幕设置入口 | List Item | 点击进入屏幕设置页 |

列表平铺展示，超出屏幕高度时可滚动。

### 音量设置页 / 屏幕设置页

暂不设计页面内容，仅作为占位页面，左上角有返回按钮。

---

## 设备管理

- 设备支持动态添加和删除（通过 Shell 命令）
- 默认启动时创建一个普通灯设备
- 设备页顺序通过 Shell 命令调整，第一页为首页
- 所有设备类型均支持多实例

### 设备类型

| 类型 | 继承关系 | 功能 |
|------|----------|------|
| 普通灯（light） | 基类 | 开关控制 |
| 色温灯（cct_light） | 继承普通灯 | 开关 + 色温调节 |
| 电动窗帘（curtain） | 独立 | 开/关/停止 + 开合度 |

---

## 数据模型

### 系统模型（system）

| 字段 | 类型 | 读写 | 说明 |
|------|------|------|------|
| brightness | int | 读写 | 屏幕亮度（0~100） |
| volume | int | 读写 | 音量（0~100） |
| network_enabled | bool | 读写 | 网络开关 |

### 普通灯模型（light）

| 字段 | 类型 | 读写 | 说明 |
|------|------|------|------|
| id | int | 只读 | 设备唯一 ID，创建时分配 |
| name | string | 读写 | 设备名称 |
| onoffsta | bool | 读写 | 开关状态 |

### 色温灯模型（cct_light，继承普通灯）

| 字段 | 类型 | 读写 | 说明 |
|------|------|------|------|
| id | int | 只读 | 设备唯一 ID |
| name | string | 读写 | 设备名称 |
| onoffsta | bool | 读写 | 开关状态（继承） |
| color_temp | int | 读写 | 色温值（2700 ~ 6500） |

### 电动窗帘模型（curtain）

| 字段 | 类型 | 读写 | 说明 |
|------|------|------|------|
| id | int | 只读 | 设备唯一 ID |
| name | string | 读写 | 设备名称 |
| position | int | 读写 | 开合度（0 ~ 100） |
| command | enum | 只写 | OPEN / CLOSE / STOP |

---

## Shell 命令行接口

### 命令格式

```
<动作> <目标> [参数...]
```

### 设备管理命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `add light <name>` | 添加普通灯 | `add light 客厅灯` |
| `add cct_light <name>` | 添加色温灯 | `add cct_light 卧室灯` |
| `add curtain <name>` | 添加电动窗帘 | `add curtain 阳台窗帘` |
| `del <id>` | 删除设备，对应设备页立即从 UI 移除 | `del 2` |
| `list` | 列出所有设备及其 ID | `list` |
| `move <id> <position>` | 调整设备页顺序（position 从 0 开始） | `move 3 0` |

### 设备控制命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `set <id> onoffsta <0\|1>` | 设置开关状态 | `set 1 onoffsta 1` |
| `set <id> color_temp <value>` | 设置色温（2700~6500） | `set 2 color_temp 4000` |
| `set <id> position <value>` | 设置窗帘开合度（0~100） | `set 3 position 75` |
| `set <id> command <OPEN\|CLOSE\|STOP>` | 发送窗帘指令 | `set 3 command OPEN` |
| `get <id>` | 查询设备当前状态 | `get 1` |

### 系统控制命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `set system brightness <value>` | 设置屏幕亮度（0~100） | `set system brightness 80` |
| `set system volume <value>` | 设置音量（0~100） | `set system volume 50` |
| `set system network <0\|1>` | 设置网络开关 | `set system network 1` |
| `get system` | 查询系统状态 | `get system` |

### UI → Shell 输出格式

当 UI 操作改变设备参数时，Shell 输出如下格式的日志：

```
[UI] set <id> <field> <value>
[UI] set system <field> <value>
```

示例：
```
[UI] set 1 onoffsta 1
[UI] set 2 color_temp 3500
[UI] set system network 0
```

---

## 线程架构与业务交互

### 线程模型

本工程采用双线程架构：

| 线程 | 职责 |
|------|------|
| LVGL 线程 | 运行 LVGL 主循环，处理所有 UI 渲染和触摸事件 |
| 业务逻辑线程 | 读取 Shell 输入，维护设备数据模型，驱动 UI 刷新 |

两个线程通过 lvframe 的事件总线（`event_bus`）传递消息，**不直接共享数据结构**，避免竞态条件。

### 消息流向

```
用户触摸 UI
    │
    ▼
LVGL 线程（UI 事件回调）
    │  发送消息到业务逻辑线程
    │  MSG: SET_PROP / SYSTEM_SET
    ▼
业务逻辑线程
    │  修改设备数据模型
    │  Shell 输出属性变化日志
    └─ [UI] set <id> <field> <value>

Shell 输入指令
    │
    ▼
业务逻辑线程（解析命令）
    │  修改设备数据模型
    │  发送消息到 LVGL 线程
    │  MSG: UI_REFRESH / ADD_DEVICE / DEL_DEVICE
    ▼
LVGL 线程（UI 刷新回调）
    └─ 更新对应设备页的显示内容
```

### 消息类型定义

| 消息类型 | 方向 | 说明 |
|----------|------|------|
| `MSG_SET_PROP` | UI → 业务 | UI 操作触发属性变更（开关、色温、窗帘指令等） |
| `MSG_SYSTEM_SET` | UI → 业务 | UI 操作触发系统属性变更（网络开关等） |
| `MSG_UI_REFRESH` | 业务 → UI | 通知 UI 刷新指定设备页的显示内容 |
| `MSG_ADD_DEVICE` | 业务 → UI | 通知 UI 新增一个设备页 |
| `MSG_DEL_DEVICE` | 业务 → UI | 通知 UI 删除指定设备页 |
| `MSG_MOVE_DEVICE` | 业务 → UI | 通知 UI 调整设备页顺序 |

### 设备生命周期管理

添加和删除设备是两个特殊操作，需要同时管理数据模型和 UI 页面，且必须保证顺序安全。

**添加设备流程：**
1. 业务逻辑线程创建设备数据模型，分配唯一 ID
2. 发送 `MSG_ADD_DEVICE`（携带设备 ID 和类型）到 LVGL 线程
3. LVGL 线程在主循环中创建对应设备页，页面持有设备 ID（不持有模型指针）
4. 设备页通过 ID 向业务逻辑线程查询数据，不直接访问模型内存

**删除设备流程（关键：防止 UI 访问已释放的模型）：**
1. 业务逻辑线程发送 `MSG_DEL_DEVICE`（携带设备 ID）到 LVGL 线程
2. **等待 LVGL 线程确认页面已销毁**（同步点，防止 UI 回调访问悬空指针）
3. LVGL 线程销毁对应设备页及其所有 UI 控件，回复确认
4. 业务逻辑线程收到确认后，释放设备数据模型内存

> 删除时必须先销毁 UI 页面，再释放数据模型，顺序不可颠倒。

### UI 页面与数据模型的解耦原则

- UI 页面**只持有设备 ID**，不持有模型指针
- UI 需要读取数据时，读取业务逻辑线程维护的只读数据快照（业务线程在修改模型后同步更新快照）
- 业务逻辑线程是数据模型的唯一所有者，负责其创建和释放
- LVGL 线程是 UI 控件的唯一所有者，负责其创建和销毁

---

## 资源清单

当前版本无图片资源，字体使用 LVGL 内置字体。

---

## 字体引擎评估：FreeType vs TinyTTF

### 评估背景

LVGL 9.x 内置支持两种矢量字体引擎：

- **TinyTTF**：基于 stb_truetype，单头文件，零外部依赖，随 LVGL 源码一同编译
- **FreeType**：独立字体渲染库，需要系统预装或交叉编译，功能完整，工业级

box86 项目最终选用 FreeType。本章记录两者的详细评估，供后续项目选型参考。

---

### 1. 代码量与集成复杂度

| 指标 | TinyTTF（stb_truetype） | FreeType |
|------|------------------------|----------|
| 集成方式 | 单头文件，随 LVGL 源码编译 | 外部库，需安装或交叉编译 |
| LVGL 集成代码量 | `lv_tiny_ttf.c` 约 717 行 | `lv_freetype.c` 等共约 1700 行 |
| 引擎自身代码量 | `stb_truetype_htcw.h` 约 5600 行（含注释） | FreeType 库本身约 20 万行（含所有模块） |
| 依赖管理 | 无外部依赖 | 需要 libfreetype（共享库约 795KB） |
| CMake 配置 | 仅开启 `LV_USE_TINY_TTF`，无额外步骤 | 需配置头文件路径、链接 libfreetype |
| 移植到新平台 | 极简，几乎零配置 | 需确认目标平台预装或交叉编译 freetype |

**结论**：TinyTTF 集成更简单，适合快速原型；FreeType 集成复杂度较高，但可通过工程内置头文件（`third_party/freetype2/`）降低配置门槛。

---

### 2. 字体格式支持

| 格式 | TinyTTF（stb_truetype） | FreeType |
|------|------------------------|----------|
| TTF（TrueType，glyf 轮廓） | ✅ 完整支持 | ✅ 完整支持 |
| OTF/CFF（PostScript 轮廓） | ⚠️ 部分支持：能解析 CFF 数据结构，但光栅化路径存在断言缺陷（`STBTT_assert(0)`），实际渲染汉字时输出方块 | ✅ 完整支持 |
| TTC（字体集合） | ⚠️ 部分：索引获取有 @TODO 注释 | ✅ 完整支持 |
| CJK 高字节映射（cmap format 2） | ❌ 明确标注 `@TODO: high-byte mapping for japanese/chinese/korean`，汉字可能无法正确映射 | ✅ 完整支持 |
| 可变字体（Variable Fonts） | ❌ 不支持 | ✅ 支持（FreeType 2.10+） |
| 字体 Hinting | 基础支持（autohint） | 完整支持（TrueType hinting + autohint） |
| SVG/COLR 彩色字体 | ❌ | ✅（需开启对应模块） |

**关键发现**：box86 使用的思源宋体（`SourceHanSerifCN-Regular.otf`）是 **CFF/OTF 格式**。stb_truetype 虽然有 CFF 解析代码，但光栅化路径对 CJK 汉字存在 `STBTT_assert(0)` 断言，实际测试中所有汉字均渲染为方块。

---

### 3. 内存消耗

#### 3.1 代码段（Flash/ROM）

| | TinyTTF | FreeType |
|--|---------|---------|
| 引擎代码段（估算） | ~50–80 KB（编译进可执行文件） | 共享库约 795 KB；若静态链接约 300–500 KB（可通过模块裁剪） |

TinyTTF 作为单头文件编译进应用，没有动态库开销；FreeType 作为共享库仅在内存中加载一份，多进程可共享。

#### 3.2 堆内存

| | TinyTTF | FreeType |
|--|---------|---------|
| 字体加载（解析 OTF 头） | 字体文件全部载入内存，汉字 OTF 约 10–20 MB | 惰性加载，按需解析字形，常驻内存约 2–5 MB |
| 字形缓存 | 依赖 LVGL 的 glyph cache（LRU） | 内置缓存（Cache Manager），可精细配置 |
| 每字形渲染临时内存 | 约 1–4 KB/字形 | 约 2–8 KB/字形（轮廓处理更复杂） |

#### 3.3 栈内存

| | TinyTTF | FreeType |
|--|---------|---------|
| LVGL 渲染线程栈需求 | 约 8–16 KB 即可（官方建议 16 KB） | 官方要求最低 32 KB，建议 64 KB |
| 原因 | 渲染逻辑简单，调用栈浅 | 内部模块嵌套深（hinting、outline decompose 等） |

> box86 工程中 `LV_DRAW_THREAD_STACK_SIZE` 已从默认值改为 `(64 * 1024)` 以满足 FreeType 需求。

**嵌入式 Linux 建议**：RAM 紧张（<32 MB）时优先 TinyTTF；RAM 充裕时 FreeType 因惰性加载反而更节省常驻内存。

---

### 4. 汉字渲染速度

以下为理论分析（无法在当前 x86 SDL 环境直接代表嵌入式 Linux 性能）：

| 场景 | TinyTTF | FreeType |
|------|---------|---------|
| 首次渲染单个汉字 | 快（无 hinting，光栅化路径简单） | 较慢（hinting 计算 + outline decompose） |
| 重复渲染（缓存命中） | 相同（均依赖 LVGL glyph cache，内存命中后无差异） | 相同 |
| 大字号（>40px） | 快，outline 精度一般 | 稍慢，但轮廓质量更高 |
| 小字号（<16px） | 边缘锯齿明显（无 hinting） | 清晰（hinting 补偿低分辨率失真） |
| 批量渲染（屏幕刷新） | 快约 20–40%（参考 stb_truetype 社区 benchmark） | 基准速度，但嵌入式平台有硬件加速（如 RK3506 的 RGA）可补偿 |

**嵌入式 Linux 特别说明**：

- RK3506 等 Rockchip 平台搭配 LVGL 时，渲染瓶颈通常是 **GPU/RGA blending** 而非字体光栅化，字体引擎本身的速度差异影响有限
- 汉字字形缓存（16px 字体约 3500 个常用汉字 × 每字形约 256 字节 = ~900 KB）是主要内存开销，与引擎选择无关
- FreeType 支持 **FT_Library** 全局复用，多线程场景下需要互斥锁（LVGL 的 `lv_ftsystem.c` 已处理）

---

### 5. 综合对比表

| 维度 | TinyTTF | FreeType | 胜出 |
|------|---------|---------|------|
| 集成复杂度 | ★★★★★ 极简 | ★★★☆☆ 中等 | TinyTTF |
| 外部依赖 | 无 | libfreetype | TinyTTF |
| TTF 格式支持 | 完整 | 完整 | 平 |
| OTF/CFF 格式支持 | 缺陷（汉字渲染为方块） | 完整 | **FreeType** |
| CJK 字符支持 | 存在已知 @TODO 缺陷 | 完整 | **FreeType** |
| 代码段占用 | 小（~80 KB） | 大（~800 KB so） | TinyTTF |
| 常驻堆内存 | 较多（全量加载） | 较少（惰性加载） | **FreeType** |
| 栈内存需求 | 低（~16 KB） | 高（~64 KB） | TinyTTF |
| 渲染速度（无缓存） | 快 | 中等 | TinyTTF |
| 小字号渲染质量 | 一般（无 hinting） | 优（完整 hinting） | **FreeType** |
| 可变字体 | 不支持 | 支持 | **FreeType** |
| 嵌入式 Linux 适用性 | 适合 RAM < 16 MB 场景 | 适合 RAM ≥ 32 MB 场景 | 视硬件 |

---

### 6. 选型建议

| 场景 | 推荐引擎 | 原因 |
|------|---------|------|
| 仅显示 ASCII / Latin 字符 | TinyTTF | 无需 CJK 支持，集成简单，资源占用小 |
| 显示中文，字体为 TTF 格式 | TinyTTF（谨慎）或 FreeType | 需实测 cmap 格式是否兼容；建议直接用 FreeType 规避风险 |
| 显示中文，字体为 OTF/CFF 格式 | **FreeType** | TinyTTF 存在已知光栅化缺陷，无法正确渲染 |
| MCU（无 OS，RAM < 8 MB） | TinyTTF | FreeType 体积过大，移植复杂 |
| 嵌入式 Linux（RAM ≥ 32 MB） | **FreeType** | 系统通常预装，共享库节省整体内存，质量更高 |
| 对渲染质量要求高（UI 设计稿还原） | **FreeType** | hinting 支持，小字号清晰 |

**box86 选型结论**：使用 FreeType，原因如下：
1. 字体文件为 OTF/CFF 格式（思源宋体），TinyTTF 无法正确渲染
2. 目标平台 RK3506 运行 Linux，RAM 充足，系统可预装 libfreetype
3. 中文界面对渲染质量有要求，FreeType 的 hinting 在小字号下效果更好
